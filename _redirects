# The "Submit a Meetup" / "Meetups" page was folded into the Events tab on
# /feeds (lb-v37). Old URLs land there directly.
/newevent       /feeds#events   301
/newevent.html  /feeds#events   301
/meetups        /feeds#events   301
/meetups.html   /feeds#events   301

# The merch storefront was folded into the Marketplace tab on /feeds as its
# "Show Merch" section (lb-v48). /merch is a spoken URL, so it keeps working.
/merch          /feeds#market   301
/merch.html     /feeds#market   301
